name: Sub Stream Final
on:
  workflow_dispatch:

jobs:
  run-stream:
    runs-on: ubuntu-latest
    steps:
      - name: Setup
        run: sudo apt-get update && sudo apt-get install -y ffmpeg xvfb chromium-browser

      - name: Launch
        run: |
          Xvfb :99 -screen 0 1280x720x24 &
          export DISPLAY=:99
          sleep 5
          
          # Change YOUR_CHANNEL_ID below
          chromium-browser --no-sandbox --window-size=1280,720 --app="https://socialcounts.org/youtube-live-subscriber-count/UCuVDdo8St9REe8waEG8RVkA" &
          sleep 20

          # Safe format: 720p, 30fps, standard bitrate
          ffmpeg -f x11grab -video_size 1280x720 -framerate 30 -i :99 \
            -vcodec libx264 -pix_fmt yuv420p -preset superfast -g 60 -b:v 2500k \
            -f flv "rtmp://a.rtmp.youtube.com/live2/${{ secrets.STREAM_KEY }}"
